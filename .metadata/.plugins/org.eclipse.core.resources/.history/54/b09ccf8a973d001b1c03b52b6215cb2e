
public class TipagemDeVariaveis {

	public static void main (String[] args) {
		
		// Estes são os tipos de variáveis primitivos que existem no java:
		int idade = 25;
		double salario = 1500.90;
		float pi = 3.14f; // No tipo float, precisamos informar explicitamente que o número atribuído à variável é um float, colocando um "f" no final.
		long inteiroLongo = 234234234l; // O mesmo acontece para o tipo long. Precisamos declarar que a atribuição é um tipo long com o "l" no final. Long é usado para números muito grandes.
		short inteiroPequeno = 1500; // O short é usado para números pequenos.
		byte inteiroMenorAinda = 122; // Byte é usado para números muito pequenos, até 256.
		
		System.out.println("Olá, a minha idade é "+ idade + " e eu ganho R$" + salario + " por mês.");
		System.out.println(pi);
		System.out.println(inteiroLongo);
		System.out.println(inteiroPequeno);
		System.out.println(inteiroMenorAinda);
		
		// Não se usa o tipo double para fazer operações com os números em algumas situações. Veja o resultado da soma abaixo e pesquise ela no google.
		double numero1 = 0.1;
		double numero2 = 0.2;
		System.out.println(numero1 + numero2);
		
		// Pense na seguinte situação: quando queremos converter um tipo int para um double, não existe problema, pois o tipo double comporta o int. Veja abaixo:
		int intQualquer = 5;
		double doubleQualquer = intQualquer;
		System.out.println(doubleQualquer);
		
		// Não deu problema nenhum. Mas, quando queremos converter um double para int, aí sim o compilador reclama, pois uma variável do tipo double não cabe numa do tipo int.
		
		// Se quisermos forçar essa atribuição, cientes de que perderemos informação na conversão, usamos o conceito de cast. Veja abaixo:
		double doubleTemperatura = 24.5;
		int intTemperatura = (int) doubleTemperatura;
		System.out.println(intTemperatura);
		
		// Percebeu? Perdemos a casa decimal na conversão, que foi feita utilizando o cast (int).
		
		// Estas situações são criadas por conta do tamanho de cada tipo de variável do java, conforme a tabela abaixo:

		// TIPO	    TAMANHO
		// boolean	1 bit
		// byte	    1 byte
		// short	2 bytes
		// char	    2 bytes
		// int	    4 bytes
		// float	4 bytes
		// long	    8 bytes
		// double	8 bytes
	}
}
